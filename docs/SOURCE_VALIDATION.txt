NSP Controller 1.4.0 static validation scope

- C# delimiter/string/comment lexical validation
- XML parsing: App.config, project file and manifest
- PostgreSQL schema/reference consistency checks
- API route and JSON field audit against current Edge source
- forbidden business ownership scan
- Reader Port terminology scan
- duplicate type/method name scan
- archive integrity and SHA-256 generation

Expected API fields:
- Reader config: ports[].port_no
- Reader status: ports[]
- Parking detection: serial_number + port_no
- Lane Calibration event: serial_number + port_no

No .NET SDK/MSBuild compiler is available in the analysis environment. A Release x86 and x64 build on Windows remains required before deployment.
